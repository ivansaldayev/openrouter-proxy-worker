{
  "name": "openrouter-proxy-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --var VERSION:$(git rev-parse --short HEAD)",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "check": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "@types/node": "^26.4.1",
    "typescript": "^5.5.2",
    "vitest": "~4.1.0",
    "wrangler": "^4.128.0"
  }
}